-- reqs-admin-events.sql
SELECT l.ENTRY
FROM LOG l
WHERE l.line IN (
SELECT line
FROM CTX
WHERE line >= (
SELECT min(line)
FROM CTX
WHERE
ASSESS_CTX IS NOT NULL AND ASSESS_CTX != 'null'
)
AND THREAD LIKE 'REACTOR-HTTP-NIO-%'

UNION
SELECT LINE
FROM CRUMB

UNION
SELECT LINE
FROM HTTP
)